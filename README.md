# roblox-game-stats

Live play/visit stats for [habib.one](https://habib.one) projects, refreshed hourly by GitHub Actions.

`stats.json` is fetched client-side from `raw.githubusercontent.com`, which serves public files with `Access-Control-Allow-Origin: *`.

To track a new game, add its slug and universeId to `games.json`.
